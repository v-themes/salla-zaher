{#
| Variable | Type | Description |
|--------------------------------|---------
|------------------------------------------------------------------------------|
| component | object | Contains merchant settings for fields from twilight.json `component` section |
| component.offer_image | image | list of banners @see views/pages/home/grid_banners.twig |
| component.offers_url | url | list of banners @see views/pages/home/grid_banners.twig |
#}
<section class="s-block s-block-offers-collections bg-[#FCFBFC]">
    <div class="flex flex-col-reverse sm:flex-row max-w-[1100px] mx-auto">
        <div class="basis-1/2 lg:basis-[40%] flex flex-col items-center justify-center gap-2 sm:gap-4 py-6 sm:py-0">
            <h2 class="font-extrabold text-2xl md:text-3xl text-slate-900">
                {{ component.offer_title }}
            </h2>
            <p class="text-sm md:text-base text-gray-500 my-3 text-center">
                {{ component.offer_descriptions }}</p>
            <a href="{{ component.offer_category[0].url }}"
                class="btn-offer px-14 pt-1 pb-2 mt-1 bg-primary rounded-sm text-white font-semibold">{{
                component.offers_url_name
                }}</a>
        </div>
        <div class="basis-1/2 lg:basis-[60%] product-details">
            <img class="object-cover h-auto lg:h-[570px] max-h-[570px] mx-auto" src="{{ component.offers_image }}" />
            <div class="container">
                {% for product in component.products %}
                <div class="product-price">
                    <a href="{{ product.url }}">
                        <div class="product-price" style="background-color: {{ product.color }}">
                            <p>{{ product.price }}</p>
                        </div>
                    </a>
                </div>
                {% endfor %}
            </div>
        </div>

    </div>
</section>