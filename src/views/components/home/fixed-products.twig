{#
| Variable | Type | Description |
|-----------------|------------|------------------------------------------------------------------|
| display_all_url | ?string | If it's existed, mean should show load more link to this url |
| title | string | |
| products | Products[] | |
| position | Int | Sorting number start from zero |
#}
{% set is_vertical = theme.settings.get('vertical_fixed_products') %}
<section class="s-block container relative z-[1]">
  {% if title %}
  <div class="s-block__title relative">
    <div class="right-side">
      <h2>{{ title }}</h2>
      {# <p>عنوان فرعي توصيفي صغير</p> #}
    </div>

    <div class="display-all-btn">
      {% if display_all_url %}
      <a href="{{ display_all_url }}">
        {{ trans('blocks.home.display_all') }}
      </a>
      {% endif %}
    </div>
  </div>
  {% endif %}

  <div id="product-fixed"
    class="grid {{ is_vertical ? 'grid-cols-2 xs:grid-cols-3 sm:!grid-cols-4 lg:!grid-cols-5 xl:!grid-cols-6 gap-y-3.5 gap-x-2' : 'grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3' }}">
    {% for product in products %}
    {% include 'pages.partials.product.card' with { is_horizontal: is_vertical ? false : true, show_desc:true } %}
    {% endfor %}
  </div>
</section>